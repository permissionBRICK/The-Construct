namespace Constructd.Api.Endpoints;

public static class UpdateEndpoints
{
    // Owned by the Update feature pair. Stage 1 advertises this feature as unsupported.
    public static RouteGroupBuilder MapUpdateEndpoints(this RouteGroupBuilder api) => api;
}
