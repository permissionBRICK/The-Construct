namespace Constructd.Api.Endpoints;

public static class MediaEndpoints
{
    // Owned by the Media feature pair. Stage 1 advertises this feature as unsupported.
    public static RouteGroupBuilder MapMediaEndpoints(this RouteGroupBuilder api) => api;
}
