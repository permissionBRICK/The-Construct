namespace Constructd.Api.Endpoints;

public static class CapacityEndpoints
{
    // Owned by the Capacity feature pair. Stage 1 advertises this feature as unsupported.
    public static RouteGroupBuilder MapCapacityEndpoints(this RouteGroupBuilder api) => api;
}
