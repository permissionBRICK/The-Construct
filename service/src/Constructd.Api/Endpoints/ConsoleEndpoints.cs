namespace Constructd.Api.Endpoints;

public static class ConsoleEndpoints
{
    // Owned by the Console feature pair. Stage 1 advertises this feature as unsupported.
    public static RouteGroupBuilder MapConsoleEndpoints(this RouteGroupBuilder api) => api;
}
