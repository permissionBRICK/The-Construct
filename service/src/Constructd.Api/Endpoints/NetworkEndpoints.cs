namespace Constructd.Api.Endpoints;

public static class NetworkEndpoints
{
    // Owned by the Network feature pair. Stage 1 advertises this feature as unsupported.
    public static RouteGroupBuilder MapNetworkEndpoints(this RouteGroupBuilder api) => api;
}
